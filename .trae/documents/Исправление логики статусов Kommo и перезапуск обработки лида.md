# План расследования инцидента с лидом 20561868

По вашему поручению я проведу детальное расследование, почему лид застрял, прежде чем применять исправления.

## 1. Откат конфигурации
- Возвращаю `lib/constants/bookings.ts` к исходному состоянию (триггер только на `Sales Order sent`), чтобы исключить влияние моих изменений на чистоту эксперимента.

## 2. Сбор улик (Анализ БД)
Я создам и запущу скрипт для извлечения истории событий по этому бронированию:
- **Таблица `booking_timeline_events`**: Проверю, зафиксировала ли система переход в статус `98035992` ("Sales Order sent").
  - *Если записи нет*: Вебхук от Kommo не дошел до сервера или упал до обработки.
  - *Если запись есть*: Значит, вебхук прошел, но условие запуска Zoho не сработало или возникла ошибка при создании заказа.
- **Таблица `kommo_webhook_events`**: Попробую найти сырой лог входящего вебхука для этого лида.

## 3. Анализ и отчет
На основе полученных данных я смогу точно сказать:
- Был ли это сбой сети (потерянный вебхук).
- Или логическая ошибка (вебхук был, но код решил ничего не делать).

## 4. Исправление
После подтверждения причины я выполню принудительный перезапуск обработки (как планировалось ранее), чтобы завершить создание Sales Order.
